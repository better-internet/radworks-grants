/*
  This is a simple JS library that adds two number together.
  This is used as an example of how to create an npm library
  and release package through Radicle.
*/

function add(x, y) {
  return x + y
}

module.exports = add
